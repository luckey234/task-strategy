import { Routes } from '@angular/router';
import { ComponentnScrollComponent } from './componentn-scroll/componentn-scroll.component';

export const routes: Routes = [
    {
        path:'',component:ComponentnScrollComponent
    }
];
